---
slug: /
sidebar_position: 1
---

# Introduction

Attach arbitrary cached values to Eloquent model instances or classes using
any Laravel cache driver. By default each authenticated user gets an
isolated cache namespace, so two users never share the same cached value
for the same model.

Continue to [Installation](./installation.md) to get started.
